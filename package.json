{
  "name": "poe-pricer",
  "version": "2.0.2",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder .",
    "clean-build": "rm -rf dist; electron-builder .",
    "ship": "git push && build --win -p always"
  },
  "author": "mahhov1",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "bs-better-stream": "^1.9.0",
    "env-paths": "^2.2.0",
    "google-oauth2-x": "^1.0.0",
    "js-desktop-base": "^3.1.2",
    "xx-element": "^2.0.2"
  },
  "devDependencies": {
    "electron": "^8.1.1",
    "electron-builder": "^20.39.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mahhov/poe-pricer.git"
  },
  "bugs": {
    "url": "https://github.com/mahhov/poe-pricer/issues"
  },
  "homepage": "https://github.com/mahhov/poe-pricer#readme",
  "description": "",
  "build": {
    "asar": false,
    "artifactName": "${name}-${version}-${os}-${arch}.${ext}",
    "files": [
      "resources",
      "src"
    ],
    "icon": "./resources/icons/fa-dollar-sign-solid-256.png",
    "linux": {
      "target": "AppImage"
    }
  }
}
